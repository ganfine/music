import { IBaseConfig } from '../types'

export const baseConfig: IBaseConfig = {
  // base_url: process.env.NODE_ENV === 'production' ? '/' : ' https://editorial.sve.cc',
  base_url: ' https://editorial.sve.cc',
  app_id: '1004',
  web_title: 'Good Life Magazine',
  name: 'allsaints'
}
